---
interface Props {
  href: string;
}

const { href } = Astro.props;
---

<a
  class="relative flex w-full min-w-[190px] justify-center gap-2 rounded bg-primary-regular py-3 text-center leading-4 text-gray-400 shadow-md transition hover:bg-primary-light"
  href={href}><slot /></a
>
