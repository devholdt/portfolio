---
interface Props {
  href: string;
}

const { href } = Astro.props;
---

<a
  class="relative py-4 px-8 gap-3 leading-4 flex shadow-md text-gray-400 bg-primary-regular text-center gap-1 rounded hover:bg-primary-dark transition"
  href={href}><slot /></a
>
