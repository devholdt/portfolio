---
interface Props {
  title: string;
  tagline?: string;
}

const { tagline, title } = Astro.props;
---

<div
  class:list={[
    "text-md flex max-w-[768px] flex-col gap-4 sm:text-lg md:order-1 md:text-left md:text-2xl",
  ]}
>
  <div>
    <h1
      class="title font-heading text-4xl text-gray-100 dark:text-gray-500 sm:text-5xl md:text-7xl"
    >
      {title}
    </h1>
    {
      tagline && (
        <p class="tagline text-gray-200 dark:text-primary-lightest">
          {tagline}
        </p>
      )
    }
  </div>
  <slot />
</div>
